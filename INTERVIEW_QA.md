# 面试问答准备 — Minecraft 反作弊系统

> 按面试轮次组织，从 HR 面到技术终面。每个问题包含回答要点和深度提示。

---

## 第一部分：HR / 初面（项目介绍与综合素质）

### Q1：简单介绍一下这个项目

**回答框架**（60 秒版本）：
1. 一句话定位：这是一个 AI 驱动的 Minecraft 反作弊系统
2. 三层架构：游戏插件检测作弊 → Spring Boot 后端处理和分析 → Vue.js 管理面板展示
3. 亮点：集成 DeepSeek 大模型做智能分析，不仅仅是规则引擎
4. 个人角色：独立完成全部设计和编码

**关键数字**：6 种检测器、40+ API、12 张表、支持 100+ 玩家

### Q2：为什么选择做这个项目？

1. Minecraft 外挂泛滥是真实存在的问题，项目有实际应用场景
2. 想通过这个项目实践全栈开发和 AI 集成——从游戏服务器插件到 Web 管理面板，技术跨度大
3. 对"如何将 AI 应用到传统软件系统"这个问题感兴趣，反作弊是一个很好的切入点

### Q3：你在这个项目中的角色是什么？

独立开发，负责需求分析、系统设计、技术选型、全部编码、测试和文档。

### Q4：这个项目最大的挑战是什么？

挑选一个具体的挑战来讲，推荐：
- **LLM 输出格式不确定性的处理**（展示 AI 工程思维）
- **后端故障时的系统可用性和安全性平衡**（展示分布式系统思维）

### Q5：如果重新做这个项目，你会改进什么？

1. 添加单元测试和集成测试（当前主要是手动测试）
2. 引入消息队列（如 RabbitMQ）解耦作弊检测事件的发送和接收，当前 WebSocket 断线时存在数据丢失风险
3. 使用 Redis 替代本地缓存，支持多服务器部署
4. 将作弊检测从规则引擎升级为机器学习模型

---

## 第二部分：技术一面（基础能力与实现细节）

### Q6：为什么选择 Spring Boot 而不是其他后端框架？

- Spring Boot 在 Java 生态中的生态位无可替代：JPA、Security、WebSocket 深度集成
- 对于这个项目，Spring WebSocket 与 Spring MVC 的无缝集成是重要考虑
- Spring Security 的过滤器链机制让双认证体系（JWT + API Key）实现起来很自然
- 对比：如果用 Go + Gin，需要自己集成更多组件；如果用 Node.js + Express，ORM 和类型安全不如 JPA

### Q7：JWT 和 API Key 分别用在什么场景？为什么需要两种认证？

- **JWT**：用于人类管理员登录前端管理面板。无状态、有过期时间（24h）、包含角色信息（SUPER_ADMIN/ADMIN/USER）
- **API Key**：用于 Minecraft 插件 → 后端的机器间通信。长期有效、可随时更换、不携带用户信息
- **为什么不能只用一种**：如果用 JWT 替代 API Key → 插件需要模拟登录，且 token 过期需要刷新逻辑。如果用 API Key 替代 JWT → 无法区分不同管理员的身份和角色
- 两者的认证入口不同：JWT 通过 Bearer Token 在 Authorization header 传递，API Key 通过 X-Api-Key header 传递，互不干扰

### Q8：解释一下你的飞行检测器是怎么工作的？

1. 监听 `PlayerMoveEvent`，排除 `anticheat.bypass` 权限、创造/观察模式、飞行允许的玩家
2. 判断玩家是否在空中（`!player.isOnGround()`），且不在水中、不在攀爬
3. 累计空中 tick 数，检查是否有药水效果（跳跃提升、漂浮）修正阈值
4. 同时检测"悬停"——玩家在空中但垂直速度为 0（`deltaY == 0`）
5. 当 `airTicks > maxAirTicks`（默认 10）或 `hoverCount > maxHoverCount`（默认 5）时，触发警告
6. 通过 `PunishmentManager.handleCheat()` 执行渐进惩罚

### Q9：你的数据库有哪些表？设计的核心思路是什么？

12 张表，核心设计思路：
- **以玩家为中心**：`players` 表是核心实体，`cheat_records`、`punishments` 都关联到玩家
- **审计可追溯**：`audit_log` 记录所有管理员操作
- **状态机**：举报（PENDING → HANDLED/DISMISSED）、封禁（ACTIVE → EXPIRED/UNBANNED）、申诉（PENDING → APPROVED/REJECTED）
- **配置分离**：`system_settings` 存储键值对配置，`notification_rules` 存储可扩展的告警规则

### Q10：WebSocket 和 HTTP 轮询相比有什么优劣？你为什么选 WebSocket？

**WebSocket 优势**：
- 延迟：服务端可主动推送，延迟 < 100ms；HTTP 轮询最优也就 1-2 秒
- 带宽：没有重复的 HTTP header 开销
- 双向通信：后端可以向插件发送指令（BAN_PLAYER, KICK_PLAYER, CONFIG_UPDATE）

**WebSocket 挑战**：
- 需要处理断线重连 → 我实现了指数退避重连（5s→60s）
- 需要心跳保活 → 15 秒 ping/pong
- 反向代理配置复杂 → Nginx 需要特殊配置

**为什么选 WebSocket**：作弊告警的实时性要求决定了必须用推送模式。如果玩家作弊后管理员要等 2 秒才能看到告警，那作弊者可能已经造成破坏。

### Q11：你的 AI 分析是怎么实现的？LLM 返回的结果如何结构化？

1. **Prompt 设计**：系统提示词定义角色（反作弊专家）、输出格式（JSON 结构）、分析依据
2. **上下文构建**：收集玩家的作弊记录、类型分布、严重程度作为分析上下文
3. **API 调用**：通过 OpenAI 兼容协议调用 DeepSeek API
4. **结果解析**：三层策略
   - 提取 ```json 代码块
   - 定位纯 JSON 对象
   - 正则 fallback 提取关键字段
5. **缓存**：相同查询参数的响应缓存 5 分钟

### Q12：如何处理 LLM 调用失败或返回异常？

1. 超时处理：HTTP 连接超时 + 读取超时设置
2. 解析失败：三层 fallback，最终兜底返回"无法确定"的默认判定
3. 缓存：成功的解析结果缓存 5 分钟，减少重试需求
4. 优雅降级：AI 分析失败不影响作弊检测的正常运行——规则引擎照常工作

### Q13：你的前端项目结构是怎么设计的？状态管理怎么做的？

- **路由分层**：11 条路由，按功能分组（监控类：Dashboard；管理类：Players/Cheats/Punishments/Whitelist；处理类：Reports/Appeals；系统类：Settings/AuditLog/Profile）
- **API 层封装**：`api/index.js` 中 Axios 实例统一处理 baseURL、token 注入、401 自动跳转登录页
- **国际化**：vue-i18n，中文和英文两套翻译
- **状态管理**：当前项目没有用 Vuex/Pinia，因为大多数页面数据通过 API 实时获取。如果一个组件需要共享状态，通过 EventBus 传递

---

## 第三部分：技术二面（系统设计与深度思考）

### Q14：如果系统需要支持 100 台 Minecraft 服务器，你会怎么改造架构？

1. **插件端**：保持不变，每个服务器独立运行插件
2. **后端**：
   - 水平扩展：后端无状态，可以直接加实例 + Nginx 负载均衡
   - WebSocket 问题：需要 sticky session 或使用 Redis Pub/Sub 做跨实例广播
   - 数据库：读写分离（主库写，从库读），热点数据加 Redis 缓存
3. **消息队列**：作弊检测事件不再直接通过 WebSocket 发送到后端，而是先发到 MQ（如 RabbitMQ），由后端消费后持久化。这样可以削峰填谷，100 台服务器同时检测到作弊也不会压垮后端
4. **配置管理**：统一的配置中心（如 Nacos），插件从配置中心拉取检测阈值

### Q15：如何减少误判，提高检测准确性？

1. **规则层面**：阈值独立可配置 + 累积触发（单次只警告），允许针对不同服务器调整
2. **AI 辅助**：LLM 考虑玩家完整行为历史而非单点事件，减少误判
3. **反馈闭环**：申诉机制让被误判的玩家可以申诉，管理员驳回的举报可以用于调整检测参数
4. **白名单**：已知的信任玩家完全豁免，避免对"看起来可疑但实际上是高手"的误判
5. **未来**：可以基于历史数据训练一个分类模型，替代纯规则引擎

### Q16：你的系统有什么安全漏洞？怎么防范的？

坦诚讨论已知的安全风险和改进措施：

1. **默认 API Key**：已在 config.yml 中添加启动检查，使用默认 key 时发出严重警告
2. **JWT 密钥随机生成**：如果未配置 JWT_SECRET 会随机生成，重启后所有 token 失效。已添加配置文档强调生产环境必须设置
3. **API Key 明文传输**：通过 HTTP Header 传输。建议生产环境使用 HTTPS
4. **localStorage 存储 token**：存在 XSS 风险。对于内部管理面板可接受，但如果面向公网应使用 httpOnly cookie
5. **CORS 配置**：当前 allow localhost，生产环境需要改为具体域名

### Q17：如果后端数据库挂了怎么办？

当前设计中后端是单点。改进方案：
1. MySQL 主从 + 自动故障转移（如 Orchestrator）
2. 插件端的本地缓存可以在数据库故障期间继续工作（30 秒刷新一次，已有降级策略）
3. 作弊检测本身在插件端运行，不依赖数据库——数据库只影响历史记录查询和管理面板

### Q18：你的 AI 分析有没有考虑过成本控制？

是的，有几层控制：
1. **5 分钟缓存**：相同查询不重复调用 API
2. **按需调用**：AI 分析是手动触发或定时触发，不是每次检测都调用
3. **可配置模型**：可以切换到更便宜的模型（如 DeepSeek-Chat vs DeepSeek-Reasoner）
4. **可关闭**：AI 功能可以通过配置完全禁用

### Q19：如果让你把这个项目部署到生产环境，还需要做什么？

1. **安全加固**：强制 HTTPS、修改所有默认密码和密钥、最小权限数据库账号
2. **监控告警**：接入 Prometheus + Grafana 监控 JVM 指标、API 延迟、错误率
3. **日志收集**：ELK 或 Loki 集中收集插件端和后端日志
4. **CI/CD**：GitHub Actions 自动构建 + 测试 + 部署
5. **备份策略**：MySQL 定时备份
6. **压力测试**：JMeter 或 k6 测试 API 并发能力
7. **灰度发布**：先部署到测试服务器，验证无误后再全量

---

## 第四部分：行为面（软技能与团队协作）

### Q20：项目中遇到的最大困难是什么？

推荐讲 **LLM 输出解析** 这个挑战：
- 问题：LLM 返回格式不稳定
- 尝试：多种方案的迭代
- 解决：三层 fallback 策略
- 收获：AI 工程需要容错思维

这个故事展示了：技术深度、问题解决能力、工程思维。

### Q21：你平时怎么学习新技术？

1. 看官方文档 + 动手实践（这个项目的所有技术都是现学的）
2. 遇到问题先自己排查（日志、源码、GitHub Issues），再搜索
3. 喜欢通过做完整项目来学习，而不是只看教程
4. 使用 AI 工具辅助开发（vibe coding），但会审查 AI 生成的代码质量

### Q22：你觉得自己代码的最大问题是什么？

坦诚 + 改进意识：
1. 缺少单元测试——当时更关注功能实现，未来会先写测试
2. 部分地方硬编码可以进一步提取为配置
3. 前端状态管理可以更规范（引入 Pinia）

### Q23：如何看待 AI 辅助编程（vibe coding）？

1. 它是效率工具，不是替代品——能加速重复性编码，但不能替代系统设计思考
2. 我在这个项目中用它来生成样板代码、API 接口定义、配置文件等，但架构设计、技术选型、问题排查仍然需要自己判断
3. 关键是要审查和理解 AI 生成的代码——不能合并自己不懂的代码
4. AI 让一个人能完成原本需要团队协作的工作量，这是全栈开发者的超能力

---

## 第五部分：快速自查清单

面试前用这些题目自测：

- [ ] 能在 60 秒内清晰介绍项目（定位 + 架构 + 亮点 + 个人角色）
- [ ] 能画出系统架构图并解释每层的职责
- [ ] 能解释至少 3 种检测器的原理
- [ ] 能解释 JWT vs API Key 的选择理由
- [ ] 能解释 AI 分析的 4 种模式和实现细节
- [ ] 能讨论 2-3 个技术挑战和解决方案
- [ ] 能讨论至少 2 个系统设计的扩展方案
- [ ] 知道自己代码的问题和改进方向
- [ ] 准备好反问面试官的问题
