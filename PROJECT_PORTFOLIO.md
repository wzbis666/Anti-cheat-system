# Minecraft 反作弊系统 — 项目展示

> 面向求职面试的深度项目介绍，涵盖架构设计、技术选型、挑战与解决方案。

---

## 一、项目概述

一个 **AI 驱动的 Minecraft 服务器反作弊系统**，将传统规则引擎与大语言模型（DeepSeek）相结合，实现作弊行为的自动化检测、智能研判和实时告警。

**项目定位**：全栈 + AI 应用，展示从游戏插件到 Web 管理面板的完整系统设计与实现能力。

**个人角色**：独立开发，负责从需求分析、系统设计、技术选型到全部编码实现和测试。

---

## 二、技术架构

### 整体架构（三层分离）

```
Minecraft Server (Spigot 插件层)
    │ 6 种作弊检测器 + 渐进式惩罚 + 本地缓存
    │ WebSocket 客户端 → 实时上报 + 接收远程指令
    ▼
Spring Boot 后端 (业务逻辑层)
    │ REST API (40+) + JWT/API Key 双认证 + 限流
    │ WebSocket 服务端 → 接收上报 + 广播告警
    │ AI Service → DeepSeek API 调用 + 缓存 + SSE 流式
    ▼
MySQL 8.0 (数据层) ←→ Vue.js 3 前端 (展示层)
```

### 技术选型理由

| 技术 | 为什么选它 |
|------|-----------|
| **Spring Boot 3.2** | Java 生态最成熟的企业级框架；JPA 自动建表减少 DDL 维护成本；Spring WebSocket 与 Web 框架无缝集成；Spring Security 提供开箱即用的认证授权体系 |
| **Vue.js 3 + Element Plus** | Composition API 逻辑复用优于 React Class 组件；Element Plus 专为后台管理设计，表格/表单/对话框组件开箱即用；中文社区活跃 |
| **Spigot API** | Minecraft 服务端事实标准，事件驱动架构天然适配作弊检测（监听移动/攻击/挖掘事件） |
| **DeepSeek** | 国产大模型，OpenAI 兼容 API 降低迁移成本；中文理解能力优于 GPT-3.5；性价比高 |
| **WebSocket** | 作弊检测需要毫秒级告警，HTTP 轮询延迟高且浪费带宽；服务端可主动推送封禁指令到插件 |
| **JWT + API Key** | 人类管理员用 JWT（无状态、可过期），机器间通信用 API Key（长期、可撤销），职责清晰 |
| **MySQL + JPA** | 作弊记录需要关联查询（按玩家/类型/时间），关系型数据库天然支持；JPA 索引注解即生成 |

---

## 三、AI 全栈能力展示

### AI 集成架构

```
前端 AI 助手 ←─ SSE 流式 ──→ AiController
                                  │
                                  ├── AiService.analyzeCheat()    → 作弊研判
                                  ├── AiService.analyzeReport()   → 举报分析
                                  ├── AiService.analyzeDashboard() → 态势评估
                                  ├── AiService.evaluateBan()     → 封禁复审
                                  └── AiService.chat()            → 交互对话 (SSE)
                                      │
                              ┌───────┴───────┐
                              │  缓存层 (5min) │  ← 相同查询复用 LLM 结果
                              │  Prompt 模板   │  ← 系统提示词 + RAG 上下文
                              │  会话管理 (30m)│  ← 多轮对话历史
                              └───────┬───────┘
                                      │
                                 DeepSeek API
                              (OpenAI 兼容协议)
```

### 四个 LLM 应用场景

1. **作弊研判**（最核心）：将玩家近期的检测记录、违规频率、作弊类型分布作为上下文，让 LLM 输出结构化判定结果（CLEAN / SUSPICIOUS / LIKELY / CONFIRMED + 置信度 + 建议操作）

2. **举报分析**：解析玩家提交的举报文本和可选证据，判断举报是否可信，输出 CONFIRM / WARN / REJECT 建议

3. **态势评估**：综合全服作弊统计（类型分布、时间趋势、高风险玩家），输出安全洞察和优化建议

4. **封禁复审**：评估已有封禁是否适当，防止过严或过松的处罚

### AI 工程的实践细节

- **结构化输出解析**：LLM 返回的自然语言可能格式不一致。我设计了三层解析策略：优先匹配 ```json 代码块 → 其次匹配裸 JSON → 最后正则提取关键字段
- **5 分钟响应缓存**：相同查询参数（如同一玩家的作弊记录 hash）在 5 分钟内复用已解析结果，减少 API 调用成本和延迟
- **SSE 流式响应**：AI 助手对话使用 Server-Sent Events，用户可看到逐字生成过程，体验接近 ChatGPT
- **RAG 上下文增强**：对话时自动检索相关玩家数据、作弊记录作为上下文，让 LLM 回答更精准

---

## 四、核心功能详解

### 1. 作弊检测引擎

6 种检测器全部继承 `AbstractDetector`，通过 Bukkit 事件系统监听：

- **飞行检测**：统计玩家空中 tick 数，排除水中、攀爬、药水效果等合法飞行场景
- **速度检测**：计算水平移动速度并区分行走/冲刺/飞行模式，药水效果动态修正阈值
- **连点检测**：左右键独立统计，区分 PvP 高频点击与自动点击器
- **自瞄检测**：监控视角旋转角速度，检测非人类瞬时旋转（yaw > 160°/tick）
- **杀戮光环**：短时间窗口（1 秒）内攻击多个不同目标 → 典型的 KillAura 特征
- **透视检测**：统计玩家挖掘的稀有矿石占总挖掘量的比例

### 2. 渐进式惩罚系统

```
警告 (1次) → 临时封禁 (2次) → 永久封禁 (3次)
```

- 3 秒冷却窗口防止同一检测重复触发
- 白名单玩家完全豁免检测
- 封禁到期自动解封（每分钟定时任务检查）
- 永久封禁支持申诉流程

### 3. 实时通信架构

```
插件检测到作弊
    │
    ├── WebSocket → 后端 (CHEAT_DETECTED 消息)
    │                   ├── 持久化到 MySQL
    │                   ├── 广播到所有前端 WebSocket 客户端
    │                   └── 检查通知规则（条件触发告警）
    │
    ├── HTTP fallback → 后端 (WebSocket 不可用时)
    │
    └── 本地处理 → 警告/踢出玩家
```

### 4. 安全设计

- **双认证体系**：JWT（管理员登录，24h 过期）+ API Key（插件通信，常量时间比较防时序攻击）
- **BCrypt 密码哈希**：管理员密码不存明文
- **IP 级别限流**：登录 15 分钟 10 次、注册 60 分钟 5 次
- **CORS 白名单**：仅允许已知前端地址跨域
- **审计日志**：所有敏感操作全量记录

---

## 五、挑战与解决方案

### 挑战 1：LLM 输出格式的不确定性

**背景**：DeepSeek API 返回自然语言，需要从中提取结构化的作弊判定（等级、置信度、建议操作）。但 LLM 有时会输出额外解释、缺少字段、或使用不同的 JSON 结构。

**尝试过的方案**：
- 直接 JSON.parse 整个响应 → 格式不稳定，频繁报错
- 使用 Function Calling → DeepSeek 早期版本不稳定

**最终方案**：三层解析策略
1. 尝试提取 ```json 代码块并解析
2. 尝试在响应中定位第一个完整 JSON 对象并解析
3. 正则表达式提取关键字段（等级、置信度）作为最终兜底

同时引入 5 分钟响应缓存，相同查询直接复用成功解析的结果。

**收获**：LLM 集成不能依赖"模型一定会按格式返回"的假设，必须有容错设计。多层 fallback 是 LLM 应用的基本模式。

### 挑战 2：后端故障时的系统可用性

**背景**：后端负责封禁状态查询、作弊记录存储、配置同步。如果后端挂了，插件端如何处理？

**尝试过的方案**：
- 直接踢出所有玩家（"安全优先"）→ 后端短暂重启就会导致全面掉线，不可接受
- 完全放行（"可用性优先"）→ 被封禁玩家换个 IP 就能绕过

**最终方案**：可配置的三级降级策略
- `allow`（默认）：放行所有玩家，后台异步重试验证
- `deny`：拒绝所有玩家，适用于高安全需求场景
- `whitelist_only`：仅白名单玩家可进入

插件端维护 30 秒刷新的本地封禁/白名单缓存，即使后端不可用也能使用缓存数据判断。

**收获**：分布式系统中任何服务都可能不可用，CAP 理论在实践中的体现——需要在安全性和可用性之间做显式的、可配置的权衡。

### 挑战 3：检测阈值的误判率平衡

**背景**：阈值太严 → 正常玩家被误封（PvP 高手 CPS 可达 12-15）。阈值太松 → 作弊者漏网。

**解决方案**：
- 每个检测器独立可配置阈值，支持运行时热更新
- 引入次数累积机制——单次触发只警告不封禁
- AI 分析作为二次确认，对历史检测记录进行上下文感知的综合研判
- 白名单机制保护信任玩家

**收获**：反作弊本质上是分类问题，永远有 FP/FN 的权衡。规则引擎适合实时检测，AI 适合深度分析，两者互补。

---

## 六、项目收获

1. **全栈系统设计能力**：从需求分析到数据库设计，从 API 设计到前端展示，完整走通了一个系统的全部环节
2. **AI 工程实践**：学会了如何将 LLM 集成到生产系统中——包括 prompt 设计、结构化输出解析、缓存策略、流式响应
3. **实时通信架构**：理解了 WebSocket 在实际项目中的应用模式——心跳、重连、降级、广播
4. **安全思维**：JWT 与 API Key 的适用场景差异、密码哈希、时序攻击防御、纵深防御
5. **工程质量**：通过系统性代码审查（47 项测试），学会了如何识别和分级处理技术债务

---

## 简历要点

以下内容可直接用于简历中的项目经历描述：

**项目名称**：AI 驱动的 Minecraft 反作弊系统（全栈独立开发）

- 设计并实现了一个三层架构的反作弊系统，包含 Spigot 游戏插件、Spring Boot 后端（40+ REST API）和 Vue.js 3 管理面板，支持 6 种作弊行为的实时检测和渐进式惩罚
- 集成 DeepSeek 大语言模型实现作弊行为的智能研判，设计了 4 种 AI 分析模式（作弊研判/举报分析/态势评估/封禁复审），包含 LLM 结构化输出解析、5 分钟响应缓存和 SSE 流式对话
- 基于 WebSocket 构建了三层实时通信架构（插件→后端→前端），实现毫秒级作弊告警推送，设计了心跳保活、指数退避重连和三级降级策略确保高可用性
- 实现了 JWT + API Key 双认证体系、BCrypt 密码哈希、IP 级别限流、审计日志等安全机制，数据库设计包含 12 张关联表和完整索引策略

**技术栈**：Java 17, Spring Boot 3.2, Spring Security, JPA/Hibernate, MySQL 8.0, Vue.js 3, Element Plus, ECharts, WebSocket, DeepSeek API, JWT
